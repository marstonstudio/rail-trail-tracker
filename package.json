{
  "name": "rail-trail-tracker",
  "version": "1.0.0",
  "description": "Rail trail ride tracker — Node.js/Docker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "RIDES_FILE=./data/rides.json node --watch server.js",
    "fetch-polylines": "node scripts/fetch-trail-polylines.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
